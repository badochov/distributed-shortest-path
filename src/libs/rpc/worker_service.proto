syntax = "proto3";

option go_package = "github.com/badochov/distributed-shortest-path/src/libs/rpc";

service Worker {
  rpc AssignSegment(Segment) returns (Ack) {}
}

message Segment {
  int32 segmentId = 1;
}

message Ack {

}